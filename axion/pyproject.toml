[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "axion"
version = "0.1.0"
description = "量化投资：估值、仓位与执行"
requires-python = ">=3.12"
license = "MIT"

[tool.hatch.build.targets.wheel]
packages = ["axion"]
